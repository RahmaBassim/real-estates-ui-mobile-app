import 'package:flutter/material.dart';

class Advs extends StatelessWidget {
  const Advs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
